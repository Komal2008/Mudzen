import { en } from './en';
import { hi } from './hi';
import { mr } from './mr';

export type Language = 'en' | 'hi' | 'mr';

export const translations = {
  en,
  hi,
  mr,
};

export const languageNames = {
  en: 'English',
  hi: 'हिंदी',
  mr: 'मराठी',
};
